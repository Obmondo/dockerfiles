name: Push docker Image to GHCR

on:
  push:
    branches:
      - airgapped  # Trigger on push to main branch, modify as needed

jobs:
  docker-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Pull Velero image from Docker Hub
        run: docker pull memcached:1.6.36-alpine

      - name: Tag image for GHCR
        run: docker tag memcached:1.6.36-alpine ghcr.io/obmondo/dockerfiles/repository-mirror:1.6.36-alpine

      - name: Push image to GHCR
        run: docker push ghcr.io/obmondo/dockerfiles/repository-mirror:1.6.36-alpine
